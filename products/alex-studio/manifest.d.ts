declare const manifest: {
  id: string;
  name: string;
  version: string;
  route: string;
  description: string;
  enabled: boolean;
  seo: { title: string; description: string };
};

export default manifest;